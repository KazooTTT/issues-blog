# 净化 GitHub 风格的文章与讨论内容

博客渲染 GitHub Flavored Markdown，并只允许 GitHub 写作中常用的安全 HTML 子集，例如 `details`、`summary` 与 `kbd`。文章和讨论中的脚本、内联事件、危险 URL、iframe 及其他任意嵌入都会被过滤；我们接受少量原始 HTML 无法原样呈现，以避免不受信任的评论或内容向静态页面注入可执行代码。
